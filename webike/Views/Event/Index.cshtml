@model IEnumerable<webike.Models.Event>

@{
    ViewData["Title"] = "Index";
}

 <div class="flexbox-container flexbox-containerLanding">

        <div class="flexbox-item flexbox-itemLanding1">
            <h1 class="display-4">Oefeningen</h1>

        </div>

    </div>

<p>
    <a asp-action="Create">Create New</a>
</p>

<div class="flexbox-container flexbox-containerEvent">
    @foreach (var item in Model) {
        <div class="flexbox-item flexbox-itemEvent2">
            <button type="button" class="btn btn-link">Oefeningnaam</button>
        </div>













    }
        @* <div class="flexbox-item flexbox-itemEvent2">
            <button type="button" class="btn btn-link">Oefeningnaam</button>
        </div>

        <div class="flexbox-item flexbox-itemEvent3">
            <button type="button" class="btn btn-link">Oefeningnaam</button>
        </div>

        <div class="flexbox-item flexbox-itemEvent4">
            <button type="button" class="btn btn-link">Oefeningnaam</button>
        </div>*@

        <div class="flexbox-item flexbox-itemEvent5">
            <select name="filterDropDown" id="filterDropDown">
                <option value="All">Alle oefeningen</option>
                <option value="Friends">Oefeningen van vrienden</option>
            </select>
        </div> 


    </div>
@* @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartLocation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Public)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EventID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EventID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EventID">Delete</a>
            </td>
        </tr>
} *@
   
